# 给另一个项目对话框的交接说明

请完整扫描我上传的 `generate-recommendation-report` Skill，不要只阅读 `SKILL.md`。

你必须一并检查：

1. `SKILL.md` 的触发条件、输入、工作流、JSON 契约和停止条件；
2. `references/report-rules.md` 的内容迁移、版式和交付检查规则；
3. `scripts/build_report.py` 的数据处理、Word 生成方式和母版依赖；
4. `scripts/verify_report.py` 的自动验证范围与仍需人工验证的部分；
5. `assets/reference.docx` 的页面、样式、页眉页脚、占位内容和结构；
6. `assets/preview.png` 的目标视觉效果；
7. `agents/openai.yaml` 的入口配置。

扫描后请先输出：

- 当前 Skill 的端到端处理流程；
- 网站化时必须保持不变的质量规则；
- 依赖 Codex、Microsoft Word 或本机环境的部分；
- 可以直接迁移到网站后端的部分；
- 需要重构或替代的部分；
- 真实格式简历上传、解析、人工确认、报告生成、验证和下载的风险清单；
- 一个分阶段的网站化方案。

在完成上述分析前，不要修改 Skill，也不要开始写网站代码。
